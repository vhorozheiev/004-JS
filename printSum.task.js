/* Write a JS program to compute and print 
the sum of the two given integers  */

let a = +prompt("enter a firtst number", 0);
let b = +prompt("enter a second number", 0);

function printSum(a, b) {
    console.log(a + b)
}

printSum(a, b);