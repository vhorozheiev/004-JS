/* Write a JS program to compute and print 
the sum of the two given integers  */

let firtsNum = 10;
let secondNum = 1;

function printSum(a, b) {
    console.log(a + b)
}

printSum(firtsNum, secondNum);