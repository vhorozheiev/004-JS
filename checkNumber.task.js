/* Write a JS program to check 
a given integer is bigger than 100 */

let num = 10;

function checkNumber(num) {
    if (num > 100) {
        return true;
    }
    return false;
};


checkNumber(num);